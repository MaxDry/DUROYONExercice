{% extends 'base.html.twig' %}

{% block title %}Edit jobs{% endblock %}

{% block body %}
    <h1>Edit jobs</h1>

    {{ include('jobs/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('jobs_index') }}">back to list</a>

    {{ include('jobs/_delete_form.html.twig') }}
{% endblock %}
